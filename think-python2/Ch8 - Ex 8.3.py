# Write a one-line version of is_palindrome

# index of [::-1] means its gonna read it backwards
print('hannah' == 'hannah'[::-1])
# so this is checking if the string is the same FORWARDS
# as it is BACKWARDS